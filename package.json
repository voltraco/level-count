{
  "name": "level-count",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "voltraco/level-count",
  "description": "Count the KV pairs in a range, with live mode",
  "scripts": {
    "test": "tap test.js && prettier-standard '*.js' && standard"
  },
  "dependencies": {
    "level-live": "^1.0.1"
  },
  "devDependencies": {
    "level-mem": "^1.0.1",
    "prettier-standard": "^7.0.3",
    "standard": "^10.0.3",
    "tap": "^11.0.0"
  }
}
